name: 'sales_analytics'
version: '1.0.0'
config-version: 2
profile: 'sales_analytics'
model-paths: ["models"]
seed-paths: ["seeds"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"
